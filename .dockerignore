# Never ship host node_modules into the build context: pnpm's symlinks are
# platform-specific and installing fresh inside the image is what makes the
# build reproducible.
**/node_modules

**/.next
**/generated/prisma
.git
.env
.env.*
!.env.example
Dockerfile
docker-compose.yml
.dockerignore
