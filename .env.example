POSTGRES_PASSWORD=LancesLance56

WEB_PORT=3000

DATABASE_URL=postgres://nandscape:change-me@localhost:5432/app_db

# Only read by seed/seed.mjs (not the app itself) to create the first admin
# account. Leave unset to skip that step - it only ever works once, while
# the Users table is still empty.
ADMIN_EMAIL=admin@example.com
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me
