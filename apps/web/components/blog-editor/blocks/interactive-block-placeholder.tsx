// Superseded by interactive-block-editor.tsx (Phase 4's widget registry).
// Nothing imports this file anymore - the sandbox this change was made in
// couldn't delete files on the mounted drive (`rm`/`unlink` both returned
// "Operation not permitted"), so it's left as this stub. Safe to delete.
